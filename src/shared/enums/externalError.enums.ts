export enum ExternalErrorCategoryEnum {
  TIMEOUT = 'timeout',
  RATE_LIMIT = 'rate_limit',
  UNAVAILABLE = 'unavailable',
  INVALID_RESPONSE = 'invalid_response',
  INVALID_VECTOR = 'invalid_vector',
  DATABASE = 'database',
  UNKNOWN = 'unknown',
}